export const lbsToKg = (lbs) => (lbs ? lbs * 0.453592 : lbs);
export const kgToLbs = (kg) => (kg ? kg / 0.453592 : kg);
export const cmToInches = (cm) => (cm ? cm / 2.54 : cm);
export const inchesToCm = (inches) => (inches ? inches * 2.54 : inches);
